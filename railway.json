{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.api"
  },
  "deploy": {
    "startCommand": "python -u -c \"import sys; print('ALIVE', flush=True); print(sys.version, flush=True); from apps.api.main import app; print('APP OK', flush=True); import uvicorn; uvicorn.run(app, host='0.0.0.0', port=8000)\"",
    "healthcheckPath": "/api/v1/health/live",
    "healthcheckTimeout": 300
  }
}
