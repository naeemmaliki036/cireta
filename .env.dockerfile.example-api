# Docker Build Environment - example-api
# Used during Docker build process

# Environment
ENVIRONMENT=production

# These should be provided at runtime, not build time
# DATABASE_URL=
# JWT_SECRET_KEY=
# ENCRYPTION_KEY=
