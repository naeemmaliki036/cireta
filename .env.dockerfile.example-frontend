# Docker Build Environment - example-frontend
# Used during Docker build process

# API URL for server-side rendering
NEXT_PUBLIC_API_URL=http://example-api:8000

# Build-time environment
NODE_ENV=production
