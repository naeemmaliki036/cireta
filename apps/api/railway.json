{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "uvicorn apps.api.main:app --host 0.0.0.0 --port 8000 --workers 2",
    "healthcheckPath": "/api/v1/health",
    "healthcheckTimeout": 60
  }
}
